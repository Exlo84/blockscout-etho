version: 3.9

services:
  frontend:
    build:
      context: ../..
      dockerfile: ./docker/Dockerfile.frontend-etho
    restart: always
    container_name: frontend
    env_file:
      -  ../envs/common-frontend.env
    ports:
      - 3000:3000
